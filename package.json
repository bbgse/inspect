{
  "name": "@bbgse/inspect",
  "version": "5.0.6",
  "description": "Inspect utility for Node.js",
  "homepage": "https://github.com/bbgse/inspect",
  "license": "MIT",
  "author": "bbgse",
  "type": "module",
  "main": "./lib/index.js",
  "module": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/bbgse/inspect.git"
  },
  "files": [
    "lib/*"
  ],
  "scripts": {
    "test": "vitest",
    "compile": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "build": "npm run build:lib && npm run build:types",
    "build:lib": "esbuild \"src/*.ts\" --outdir=lib --bundle --platform=node --target=node18 --format=esm",
    "build:types": "tsc -p tsconfig-types.json"
  },
  "dependencies": {
    "get-func-name": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.10",
    "esbuild": "^0.19.5",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  }
}
